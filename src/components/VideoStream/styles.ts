import styled from 'styled-components'

export const Wrapper = styled.div`
  position: fixed;
  top: 0;
  right: 0;
  z-index: 191919;
  user-select: none;
  pointer-events: none;
  width: 300px;

  aspect-ratio: 16 / 9;
`
